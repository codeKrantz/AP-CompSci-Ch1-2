public class Ch2 {

    public static void main(String[] args){
        // Classes
        // Objects that we create


// The Math Class  
// this class does not need to be imported
System.out.println(Math.abs(-1)); // returns absolute value
System.out.println(Math.pow(10, 2)); // returns the base raised to a power RETURNS A DOUBLE
System.out.println(Math.sqrt(25)); // returns the square root AS A DOUBLE
System.out.println(Math.random()); //A random double value between 0 and 1 / usually an irrational number
System.out.println(Math.PI); // returns PI




/*
 * String Class Methods:
 *  Method
Return
length()
An int equal to the length of the screen
substring(int beginIndex)
A String that is a subset of the original String, starting at beginIndex through the end
substring(int beginIndex, int endIndex)
A String that is a subset of the original String, starting at beginIndex up to but not including endIndex
equals(Object anObject)
A Boolean value - if the String is the same as anObject it returns true, otherwise it returns false
compareTo(String anotherString)
An int reflecting the status after comparing two strings lexicographically
-Returns a negative integer if String comes before anotherString
-Returns 0 if String is the same as anotherString
-Returns a positive integer if String comes after anotherString
indexOf(String aString)
An int that is the index of the first occurrence of aString in the String. It will return -1 if aString does not appear in String

 */






 
    }
}